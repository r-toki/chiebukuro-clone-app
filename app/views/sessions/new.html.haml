.max-width-24.mx-auto
  %h1.text-center Log in
  = form_with(url: sessions_path, scope: :session, local: true) do |f|
    .form-group
      = f.label :name
      = f.text_field :name, class: "form-control"
    .form-group
      = f.label :password
      = f.password_field :password, class: "form-control"
    = hidden_field_tag 'redirect_back', params[:redirect_back]
    = f.submit "Log in", class: "btn btn-primary"
  %br
  Don't have an account?
  = link_to "Sign up", new_user_path(redirect_back: params[:redirect_back])
